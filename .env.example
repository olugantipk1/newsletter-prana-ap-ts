# Prana — Daily Satirical Civic Newsletter

# Use live RSS feeds for the daily edition (default: mock data only).
# Set to 1 to fetch Telugu RSS feeds and merge into the edition.
PRANA_USE_RSS=0

# Optional: AI curation (future). Used to generate Jab + bilingual TL;DR from raw items.
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=

# Optional: Cache / DB for pre-built editions (future).
# KV_REST_API_URL=
# KV_REST_API_TOKEN=
